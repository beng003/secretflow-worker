# SecretFlow Worker 计算服务

## 项目简介

SecretFlow Worker 是一个基于 SecretFlow 框架的分布式隐私计算服务，提供了完整的隐私保护计算任务执行能力。项目采用 Celery + Redis 架构，支持多种隐私计算算法，包括隐私集合求交（PSI）、联邦学习（SS-LR、SS-XGBoost）、数据预处理、统计分析和模型评估等功能。

## 核心特性

### 🔐 隐私计算能力
- **PSI（隐私集合求交）**：支持多方数据求交，保护原始数据隐私
- **联邦学习**：SS-LR（逻辑回归）、SS-XGBoost（梯度提升树）
- **安全多方计算**：基于 SPU 的密态计算引擎

### 📊 数据处理
- **预处理**：标准化、归一化、编码、分箱、缺失值填充
- **统计分析**：表统计、相关系数、方差膨胀因子（VIF）
- **模型评估**：二分类评估、回归评估

### 🚀 分布式架构
- **Celery 异步任务队列**：支持任务并发执行和失败重试
- **Redis 消息队列**：高性能任务分发和状态同步
- **Docker 容器化部署**：开箱即用，易于扩展

### 🔄 任务状态监听
- **实时状态更新**：通过 Redis 发布订阅实时推送任务状态
- **跨容器通信**：Worker 容器与 Web 容器无缝对接
- **双重保障机制**：发布订阅 + 消息队列确保状态不丢失

## 技术架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Web容器 (FastAPI)                         │
│  - 任务提交和管理                                                │
│  - 状态监听和数据库更新                                           │
│  - API接口和前端服务                                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Redis 消息队列
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   Worker容器 (SecretFlow)                        │
│  - Celery Worker                                                 │
│  - SecretFlow 计算引擎                                           │
│  - 任务执行和结果返回                                             │
└─────────────────────────────────────────────────────────────────┘
```

## 技术栈

- **计算框架**：SecretFlow 1.x
- **任务队列**：Celery 5.x
- **消息队列**：Redis 7.x
- **容器化**：Docker + Docker Compose
- **Python版本**：Python 3.10+

## 项目结构

```
secretflow-worker/
├── src/                          # 源代码目录
│   ├── base/                     # 基础任务类
│   │   ├── task_base.py         # 任务基类
│   │   └── async_helpers.py     # 异步辅助工具
│   ├── config/                   # 配置模块
│   │   ├── settings.py          # 配置管理
│   │   └── celery_config.py     # Celery配置
│   ├── secretflow_task/         # SecretFlow任务模块
│   │   ├── jobs/                # 任务实现
│   │   │   ├── psi_task.py     # PSI隐私求交
│   │   │   ├── linear/         # 线性模型
│   │   │   │   └── ss_lr_task.py
│   │   │   ├── boost/          # 集成学习
│   │   │   │   └── ss_xgb_task.py
│   │   │   ├── preprocessing/  # 数据预处理
│   │   │   │   ├── scaler/     # 标准化/归一化
│   │   │   │   ├── encoder/    # 编码
│   │   │   │   ├── binning/    # 分箱
│   │   │   │   └── fillna/     # 缺失值填充
│   │   │   ├── stats/          # 统计分析
│   │   │   │   ├── table_statistics_task.py
│   │   │   │   ├── pearson_correlation_task.py
│   │   │   │   └── vif_task.py
│   │   │   └── evaluation/     # 模型评估
│   │   │       ├── biclassification_eval_task.py
│   │   │       └── regression_eval_task.py
│   │   ├── celery_tasks.py     # Celery任务封装
│   │   ├── task_executor.py    # 任务执行器
│   │   ├── task_dispatcher.py  # 任务分发器
│   │   ├── cluster_initializer.py  # 集群初始化
│   │   └── device_manager.py   # 设备管理
│   ├── utils/                   # 工具模块
│   │   ├── log.py              # 日志工具
│   │   ├── exceptions.py       # 异常定义
│   │   └── status_notifier.py  # 状态通知
│   ├── celery_app.py           # Celery应用入口
│   └── worker.py               # Worker启动入口
├── docker/                      # Docker配置
│   ├── Dockerfile              # 镜像构建文件
│   ├── docker-compose.yml      # 开发环境编排
│   └── docker-compose.production.yml  # 生产环境编排
├── scripts/                     # 脚本工具
│   ├── send_task.py            # 任务发送工具
│   └── entrypoint.sh           # 容器启动脚本
├── tests/                       # 测试代码
│   ├── unit/                   # 单元测试
│   ├── integration/            # 集成测试
│   └── no_pytest/              # 手动测试脚本
├── docs/                        # 文档目录
│   ├── worker-api/             # Worker API文档
│   ├── summary/                # 项目总结
│   ├── 04.task_status_listener.md  # 状态监听设计
│   └── chain.md                # 任务链设计
└── requirements.txt            # Python依赖

```

## 快速开始

### 前置要求

- Docker 20.10+
- Docker Compose 2.0+
- Python 3.10+ (本地开发)

### 使用Docker部署（推荐）

1. **克隆项目**
```bash
git clone <repository-url>
cd secretflow-worker
```

2. **配置环境变量**
```bash
# 复制环境变量模板
cp config/production.env.template .env.production

# 编辑环境变量
vim .env.production
```

3. **启动服务**
```bash
# 生产环境
docker compose -f docker/docker-compose.production.yml --env-file .env.production up -d

# 开发环境
docker compose -f docker/docker-compose.yml up -d
```

4. **查看日志**
```bash
docker compose -f docker/docker-compose.production.yml logs -f
```

5. **发送测试任务**
```bash
# 发送PSI任务
python scripts/send_task.py psi

# 发送Hello测试任务
python scripts/send_task.py hello
```

### 本地开发

1. **安装依赖**
```bash
pip install -r requirements.txt
```

2. **启动Redis**
```bash
docker run -d -p 6379:6379 redis:7-alpine
```

3. **启动Worker**
```bash
# 方式1：直接启动
celery -A src.celery_app worker --loglevel=info

# 方式2：使用脚本
python src/worker.py
```

4. **发送任务**
```bash
python scripts/send_task.py psi
```

## 配置说明

### 环境变量

主要环境变量配置（`.env.production`）：

```bash
# Redis配置
REDIS_URL=redis://127.0.0.1:60379/0

# Celery配置
CELERY_BROKER_URL=redis://127.0.0.1:60379/0
CELERY_RESULT_BACKEND=redis://127.0.0.1:60379/0

# SecretFlow配置
SECRETFLOW_PARTIES=alice,bob
SECRETFLOW_ADDRESS=local

# 日志配置
LOG_LEVEL=INFO
LOG_FILE=/app/logs/worker.log

# Worker容器标识
WORKER_CONTAINER_ID=secretflow-worker
```

### 集群配置

支持两种部署模式：

#### 本地模式（开发测试）
```python
"sf_init_config": {
    "parties": ["alice", "bob"],
    "address": "local"
}
```

#### 集群模式（生产环境）
```python
"sf_init_config": {
    "parties": {
        "alice": {
            "address": "192.168.1.50:7700",
            "listen_addr": "0.0.0.0:7700"
        },
        "bob": {
            "address": "192.168.1.60:7800",
            "listen_addr": "0.0.0.0:7800"
        }
    },
    "self_party": "alice"
}
```

## API文档

详细的API文档请参考：
- [Worker API 文档](docs/worker-api/00.worker-api.md) - 完整的任务接口规范
- [Web对接指南](docs/web_integration_guide.md) - Web容器对接说明
- [任务状态监听](docs/04.task_status_listener.md) - 状态同步机制
- [任务链设计](docs/chain.md) - 任务编排方案

## 支持的任务类型

### 1. 隐私计算
- `psi` - 隐私集合求交

### 2. 机器学习
- `ss_lr` - 联邦逻辑回归
- `ss_xgb` - 联邦XGBoost

### 3. 数据预处理
- `standard_scaler` - 标准化
- `minmax_scaler` - 归一化
- `label_encoder` - 标签编码
- `onehot_encoder` - 独热编码
- `quantile_binning` - 分位数分箱
- `equal_width_binning` - 等宽分箱
- `fillna` - 缺失值填充

### 4. 统计分析
- `table_statistics` - 表统计
- `pearson_correlation` - 皮尔逊相关系数
- `vif` - 方差膨胀因子

### 5. 模型评估
- `biclassification_eval` - 二分类评估
- `regression_eval` - 回归评估

## 测试

### 运行单元测试
```bash
pytest tests/unit/ -v
```

### 运行集成测试
```bash
pytest tests/integration/ -v
```

### 手动测试
```bash
# PSI任务测试
python tests/no_pytest/local_test.py

# 多节点测试
python tests/no_pytest/local_test_alice.py  # Alice节点
python tests/no_pytest/local_test_bob.py    # Bob节点
```

## 性能指标

任务执行完成后会返回详细的性能指标：

- **总执行时间**：任务从开始到完成的总时长
- **集群初始化时间**：SecretFlow集群启动耗时
- **设备初始化时间**：PYU/SPU设备创建耗时
- **算法执行时间**：实际计算耗时
- **各阶段占比**：各环节时间占比分析

## 故障排查

### 常见问题

1. **ModuleNotFoundError: No module named 'secretflow_task'**
   - 原因：Ray worker进程找不到项目模块
   - 解决：已在 `cluster_initializer.py` 中配置 `runtime_env` 设置 `PYTHONPATH`

2. **Permission denied 错误**
   - 原因：容器内文件权限不足
   - 解决：`chmod 777 data/` 或调整 Dockerfile 中的用户权限

3. **Redis连接失败**
   - 原因：Redis未启动或配置错误
   - 解决：检查 `REDIS_URL` 配置，确保Redis服务正常运行

4. **任务超时**
   - 原因：任务执行时间过长
   - 解决：增加 `task_time_limit` 或优化数据量

详细故障排查请参考：[故障排查文档](docs/summary/23.troubleshooting.md)

## 开发指南

### 添加新任务类型

1. 在 `src/secretflow_task/jobs/` 下创建任务文件
2. 使用 `@TaskDispatcher.register_task()` 装饰器注册任务
3. 实现任务执行逻辑
4. 更新 Worker API 文档

示例：
```python
from secretflow_task.task_dispatcher import TaskDispatcher

@TaskDispatcher.register_task("my_task")
def execute_my_task(devices, task_config):
    # 实现任务逻辑
    return {"status": "success", "result": {...}}
```

### 代码规范

- 使用 Black 进行代码格式化
- 遵循 PEP 8 编码规范
- 为所有公共函数编写文档字符串
- 添加单元测试覆盖新功能

## 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 联系方式

- 项目维护者：beng003
- 邮箱：3250902461@qq.com
- 项目主页：https://github.com/beng003/secretflow-worker

## 致谢

- [SecretFlow](https://github.com/secretflow/secretflow) - 隐私计算框架
- [Celery](https://docs.celeryproject.org/) - 分布式任务队列
- [Redis](https://redis.io/) - 内存数据库

---

**注意**：本项目用于学习和研究目的，生产环境使用前请进行充分测试和安全评估。
